var pigvis = {};
pigvis.layout = {};
pigvis.layout.flow = function () {
    var hGap = 0.1,
        size = [1, 1]; // width, height

    var flow = {};
    flow.nodes = function (d) {

    };

    return flow;
};
